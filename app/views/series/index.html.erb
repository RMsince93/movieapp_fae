<div class="container">
  <div class="row row-cols-1 row-cols-md-4 g-4">
    <% @tv_shows.each do |tv_show| %>
      <div class="col mb-4">
        <%= render partial: 'shared/tv_show', locals: { tv_show: tv_show } %>
      </div>
    <% end %>
  </div>
</div>
